<%= release_manager %> started a production deployment for package `<%= package_version %>`.

<% if override_status -%>
:warning: Status check overridden! Deployment is authorized with the following message

> <%= override_reason %>

<% end -%>
---
<%= status.to_issue_body %>

---

<% if ci_pipeline_url -%>

The [deployer pipeline](<%= ci_pipeline_url %>) can be inspected on the `ops` instance only by the authorized personnel.

<% end -%>
---

:robot: This comment was generated by [`release-tools`](https://gitlab.com/gitlab-org/release-tools).
